<div class="top_image"></div><br>

<div class="well well-lg">このサイトはあなたが日々思った何気ない一言を書き綴る場です</div>
<br>

<%= link_to 'ツイートする', new_blog_path,class:"btn-info btn-lg" %><br><br>

<h1>ツイート一覧</h1><br>

<p><%= notice %></p>

<div class ="row">
  <% @blogs.each do |blog| %>
    <div class="col-xs-12 col-md-6"><%= blog.content %></div>
      <%if @current_user.id == blog.user_id%>
        <div class="col-md-2"><%= link_to "お気に入り", blog_path(blog.id) %></div>
        <div class="col-md-1"><%= link_to "編集", edit_blog_path(blog.id),data:{ confirm: '本当に編集していいですか？' } %></div>
        <div class="col-md-1"><%= link_to '削除', blog_path(blog.id), method: :delete,data: { confirm: '本当に削除していいですか？' } %></div>
      <%else%>
        <div class="col-md-2"><%= link_to "お気に入り", blog_path(blog.id) %></div>
      <% end %> 
  <% end %>  
</div>